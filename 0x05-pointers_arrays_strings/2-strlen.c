#include "main.h"
/**
 * reset_to_98 - start of my function
 * @n: integer
 * Description: pointss to a variable
 * Return: void
*/

int _strlen(char *s) {
    int length = 0;
    while (s[length] != '\0') {
        length++;

    }
     return length;
}
